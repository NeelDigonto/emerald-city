import { Button, Paper } from "@mui/material";
import React from "react";

const MaterialExplorer = ({ name }: { name: string }) => {
  return (
    <Paper>
      <Button></Button>
    </Paper>
  );
};

export default MaterialExplorer;
